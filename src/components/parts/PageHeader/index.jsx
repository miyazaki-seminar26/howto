export const PageHeader = ({ pageTitle }) => {
  return (
    <div>
      <div className="font-bold border-l-20 rounded border-l-rose-200 p-2 border-b-2 border-b-gray-200">{pageTitle}</div>
    </div>
  );
};
